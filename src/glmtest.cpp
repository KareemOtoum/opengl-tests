#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>
#include <glm/gtc/type_ptr.hpp>
#include <iostream>

int main() {
  /* glm::vec4 pos(1.f, 0.f, 0.f, 1.f);
  glm::mat4 trans = glm::mat4(1.0f);
  trans = glm::translate(trans, glm::vec3(1.f, 1.f, 0.f));
  pos = trans * pos;
  std::cout << pos.x << pos.y << pos.z << "\n"; */

  
  return 0;
}